#include "sdlinput.h"

void sdlInputInit()
{
    // sdl init
}

void sdlInputLoadConfig()
{
    // use config to set keys
}

void sdlInputSaveConfig()
{
    // use config to set keys
}

void sdlHandleKey(SDL_Event event)
{
    switch (event.key.keysym.sym) {
        case SDLK_RIGHT:
            break;
        default:
            break;
    }
}